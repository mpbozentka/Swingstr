node_modules
dist
build
.DS_Store
.env

# MediaPipe runtime assets — regenerated by `npm install` (postinstall runs
# scripts/setup-mediapipe.js), too large/binary to belong in git.
public/mediapipe/wasm
public/mediapipe/*.task
